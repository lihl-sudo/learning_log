# create database test;
# ALTER user 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'lhl666666';
# CREATE USER 'lihl'@'%' IDENTIFIED WITH mysql_native_password BY 'lhl666666';
# GRANT All privileges ON test.* TO 'lihl'@'%';
# SELECT host, user, plugin, authentication_string FROM mysql.user;
# FLUSH PRIVILEGES;
